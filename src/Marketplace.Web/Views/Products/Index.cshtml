@model IEnumerable<Product>

<div class='page'>
    <div class="title">Products</div>
    <div class="header">
        <p class='hints'>We found @Model.Count() products.</p>
    </div>
    <table>
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(m => m.Name)</th>
                <th>@Html.DisplayNameFor(m => m.Description)</th>
                <th>@Html.DisplayNameFor(m => m.Stock)</th>
                <th>@Html.DisplayNameFor(m => m.Price)</th>
                <th>
                    <a class="button" asp-controller="products" asp-action="create">
                        <i class="fa-solid fa-file-circle-plus">&nbsp;</i>
                        <span>New</span>
                    </a>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Take(13))
            {
                <tr>
                    <th>@Html.DisplayFor(m => item.Name)</th>
                    <th>@Html.DisplayFor(m => item.Description)</th>
                    <th>@Html.DisplayFor(m => item.Stock)</th>
                    <th>@Html.DisplayFor(m => item.Price)</th>
                    <td>
                        <a class="button" asp-controller="products" asp-action="edit" asp-route-id="@item.Id">
                            <i class="fa-solid fa-file-pen">&nbsp;</i>
                            <span>Edit</span>
                        </a>
                        <a class="button" asp-controller="products" asp-action="delete" asp-route-id="@item.Id">
                            <i class="fa-solid fa-trash-can">&nbsp;</i>
                            <span>Delete</span>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>